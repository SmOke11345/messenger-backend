generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users {
  id          Int     @id @default(autoincrement())
  name        String
  lastname    String?
  password    String
  profile_img String?
  login       String
  friends     Json[]
}
